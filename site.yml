---

#- hosts: all
#  sudo: yes
#  roles:
#    - common

- hosts: mesos-master
  tasks: []
  tags:
    - always

- hosts: mesos-slave
  tasks: []
  tags:
    - always

- hosts: mesos-master
  sudo: yes
  roles:
    - mesos-common
    - mesos-master
  vars:
    paas_apps_fqdn: 'paas.mozilla.community'
  tags:
    - mesos-master

- hosts: mesos-slave
  sudo: yes
  roles:
    - mesos-common
    - mesos-slave
  tags:
    - mesos-slave
